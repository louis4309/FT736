# ---------
#  9-4
# ---------
# ---------------------------------------------------------- #

a <- c(1, 1, 0, 1)
b <- c(2, 1, 0, 1)

# 這檢測a和b的每一個元素
ifelse(a == 1 & b == 1, "Yes", "No")

# 這只檢測a和b的頭一個元素,因此只回傳一個結果
ifelse(a == 1 && b == 1, "Yes", "No")

# ---------------------------------------------------------- #